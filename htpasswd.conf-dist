<?php
// $Id$

$htpasswd_maildomain = "example.com";
$htpasswd_file       = "/etc/htpasswd";
$htpasswd_groupfile  = "/etc/htgroup";

// Create a mapping from htgroups to drupal roles, e.g.
// "root"=>"administrator" 
$htpasswd_roles=array(
  "root"=>"administrator"
);
